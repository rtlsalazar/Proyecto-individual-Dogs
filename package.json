{
  "name": "henry-proyecto-individual",
  "author": "Moises Salazar",
  "cohort": "WebPT14b",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "postinstall": "(cd ./app-frontend && npm i) && (cd..) && (cd ./app-backend && npm i)",
    "build": "(cd ./app-frontend && npm run build) && (cd..) && (./cd app-backend && npm run build)"
  }
}
