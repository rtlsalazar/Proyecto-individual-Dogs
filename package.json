{
  "name": "henry-proyecto-individual",
  "author": "Moises Salazar",
  "cohort": "WebPT14b",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "postinstall": "(cd app-frontend && yarn) && (cd..) && (cd app-backend && yarn)",
    "build": "(cd app-frontend && yarn build) && (cd..) && (cd app-backend && yarn build)"
  }
}
